function Result = modulusop(firstValue,secondValue)
% Helps to do modulus operation for the given two numbers
%
% It will return the result to its caller function

Result = mod(firstValue,secondValue);
end